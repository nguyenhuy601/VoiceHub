# Biến dùng cho docker-compose (interpolation) — không đặt default trong file .yml
# Phải trùng với ý định presence giữa socket-service và user-service
USER_SERVICE_INTERNAL_TOKEN=dev-user-internal-presence-token-change-me
